-extends can convert schemas before hand
-schemas can have subschemas which must be validated too
-if a schema has a definedby link, link the schema schema to the schema
 -propagate this to properties
-change attributes names to uris, and do lookups based on schema schema.