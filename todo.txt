-write README
-choose a license
-get "describedby" links working in JSON instances
-change attributes names to uris, and do lookups based on schema schema.

Spec changes:
-Fix numbering under hyper-schema
-Make rel & href required
-Should readonly be readOnly?
-root and readonly should specify that they are booleans

Forum
-mention if supporting additionalItems, make items tuple-type only
-schemas should be versioned